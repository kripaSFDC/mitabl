name: mitabl-test-windows

services:
  backend:
    extends:
      file: ../docker-compose.yml
      service: backend

  queue-worker:
    extends:
      file: ../docker-compose.yml
      service: queue-worker

  scheduler:
    extends:
      file: ../docker-compose.yml
      service: scheduler

  db:
    extends:
      file: ../docker-compose.yml
      service: db

  redis:
    extends:
      file: ../docker-compose.yml
      service: redis

volumes:
  db_data:
  redis_data:
